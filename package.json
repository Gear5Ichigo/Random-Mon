{
  "dependencies": {
    "@pixi/sound": "^6.0.1",
    "@pixi/ui": "^2.2.2",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "pixi.js": "^8.8.1",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "esbuild": "0.25.1"
  },
  "scripts": {
      "build": "esbuild server.js --platform=node --bundle --minify --outfile=build/out.js",
      "dev": "nodemon server.js"
  }
}
